# electricity-bill-calculator--electron.js

# run - npm install to install all dependencies

# To build run:

npm run package-win (for Windows)
npm run package-mac (for MacOS)
npm run package-linux (for Linux)
